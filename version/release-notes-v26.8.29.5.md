[同步] 同步 PiliPlus 字体页面大重构，支持横竖屏自适应、字体预览和可滚动字体列表。
[同步] 同步字体设置存储模型、默认字体回退、`.ttc` 字体导入和字体设置保存修复。
[同步] 同步选择区域补丁，修复空文本选择区域处理。
[同步] 升级上游依赖锁定版本，`flutter_html` 切换至上游 Git `dev` 分支。
[修复] 兼容旧版 `customAppFont` 字体索引，将当前自定义字体迁移到 `customFont.otf`，避免升级后丢失字体设置。
[修复] 自定义字体写入完成后再应用设置，避免保存与主题刷新发生竞态。
