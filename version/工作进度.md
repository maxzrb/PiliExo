# PiliPlus 工作进度

## 2026-08-28 16:22

- Android HDR 原生 Media3 播放方案已完成首版实现。
- 已通过项目 Flutter 补丁后的 Android debug 构建，APK 位于 `build/app/outputs/flutter-apk/app-debug.apk`，约 140.08 MB。
- 设备 `AN5UUT6225004417` 已连接，但黑屏状态下未确认安装权限；用户将自行执行安装命令。
- 待解锁设备后验证 HDR10、Dolby Vision、HDR Vivid 真机播放及失败回退。

## 2026-08-28 16:53

- 已通过 ADB 查看用户真机截图，确认红屏是 `Obx` 构建期间修改状态导致的 Flutter 错误。
- 已修复 HDR/SDR 切换红屏：变换重置和画面模式同步延迟到构建完成后执行。
- 已修复返回播放列表时 SurfaceView 残帧：脱离页面时隐藏并清空原生视频 Surface，重新进入时恢复。
- 最新 APK 已重新构建，安装前请先解锁手机并允许 USB 安装。
- 验证通过：全量 Flutter 测试、Android 原生编译和 Android 单测。

## 2026-08-28 17:11

- 检查用户新截图，确认 HDR 返回播放列表时残留的是独立 `SurfaceView` 合成层，原因是路由切换早于播放器组件销毁。
- 已将 HDR 返回改为先拦截并立即清理原生 Surface，再执行路由返回；原生隐藏使用 `GONE`，重新进入时恢复可见性。
- 新 APK：`build/app/outputs/flutter-apk/app-debug.apk`，201236035 bytes，SHA-256 为 `ABCC62569508456C5B7872AB725F611BBF227B88B5AE5FDE95E9E3A9A44BCBDC`。
- 已验证全量 Flutter 测试、目标文件 analyze、Android 单测和 debug APK 构建；待用户安装后复测返回列表残留现象。

## 2026-08-28 18:18

- 所有底栏和侧栏导航项统一接入 Android 原生可调振幅震动；设置页新增 1–255 强度调节，保留原有震动开关。
- PiliExo 应用标识、描述、源码链接和更新入口已切换到 `https://github.com/maxzrb/PiliExo`；Android 更新按 Release 标签比较，ModelScope `AerithDream/PiliExo` 优先，GitHub Release 兜底。
- 已创建 GitHub fork、ModelScope 数据集并发布 `v26.8.28.1`。
- Release APK：`dist/PiliExo_android_v26.8.28.1.apk`，68,793,128 bytes，SHA-256 为 `9D64CAD5C991485E4DCB323C2DAA8FA2DC5F8B300C97EA7DD8C71190B2D5664F`。
- 验证通过：`flutter test --no-pub` 5 个测试、Android `:app:testDebugUnitTest -Pkotlin.incremental=false`、Android release 构建；完整 analyze 无 error 但有既有 info/lint。本轮未通过 ADB 安装或启动。
- 当前仍待用户真机验收振动、HDR10/Dolby Vision/HDR Vivid、切换清屏、横竖屏、画中画和长时间播放。

## 2026-08-28 18:58

- 新安装默认开启震动，默认强度改为 80；调节强度滑块时即时震动反馈，取消调节恢复临时值。
- 两个普通视频默认画质顺序调整为 8K、HDR Vivid、杜比视界、HDR 真彩及其他画质；“蜂窝网络”统一改为“移动数据”。
- 顶栏、底栏、普通页面 AppBar 和浮动底栏增加磨砂半透明外观；HDR 视频继续使用原生 SurfaceView。
- Android applicationId 改为 `com.maxzrb.piliexo`，Release 启用 R8/资源压缩并按 `arm64-v8a`、`armeabi-v7a` 分包；更新器按设备 ABI 选择对应资产。
- 关于页和应用元数据描述精简为“HDR支持的PiliPlus修改版”；修正透明主题下浮动底栏的磨砂底色。
- v26.8.28.2 Release 成品已生成：`dist/PiliExo_android_v26.8.28.2_arm64-v8a.apk`（26,601,707 bytes，SHA-256 `94EA78F3AA2458B9C56F264ED8A99BAF8971AD0390B010FABFD7C558C6B371A6`）；`dist/PiliExo_android_v26.8.28.2_armeabi-v7a.apk`（26,480,705 bytes，SHA-256 `E9234886FC779CBC424A8B6965AB256353753549FFE973EDEBAA8C18EE4B196C`）。
- 已验证 `flutter test --no-pub` 8/8、Android 单测、Release 构建和 `git diff --check`；未通过 ADB 安装或启动，待用户自行按设备 ABI 安装验收。
- 本地没有 `android/key.properties`，本轮 APK 使用 debug keystore 兜底签名；发布前需配置固定的正式签名。

## 2026-08-28 19:49

- 外观设置新增“磨砂半透明”开关，默认开启；提供轻薄、标准、浓厚三档磨砂效果，切换即时生效。
- 主页顶栏、主底栏、浮动底栏、普通页面 AppBar，以及动态/WebView/日志等特殊顶栏统一响应磨砂设置；关闭后恢复不透明背景。
- 版本递增为 `v26.8.28.3`，Release 按 `arm64-v8a`、`armeabi-v7a` 分包。
- 成品：`dist/PiliExo_android_v26.8.28.3_arm64-v8a.apk`（26,604,087 bytes，SHA-256 `2B2DEFDF6B4A8239CB0288CCFD9212279B41C256B5E719DA26FF6C06D05B01E1`）；`dist/PiliExo_android_v26.8.28.3_armeabi-v7a.apk`（26,479,865 bytes，SHA-256 `F9E46227B2ED70E07BE03614524B0C981D8EB0160376244367A2FF763C849E9A`）。
- 已验证 Flutter 测试 8/8、Flutter analyze 无 error/warning、Android 单测、Release 构建和 `git diff --check`；未通过 ADB 安装或启动，待用户自行按 ABI 安装。

## 2026-08-28 20:06

- 根据用户截图确认顶栏与状态栏之间的视觉缝隙来自顶栏顶部边框；已关闭顶栏边框，并将轻薄磨砂档透明度调为 0.64，使背景透出更多。
- 修正震动反馈设置项的默认值，解决新安装时运行默认开启但设置页开关显示关闭的问题；已有用户明确关闭的设置保持不变。
- 新增 `docs/发布流程.md`，记录测试阶段保持当前版本不变、正式发布时递增版本号和构建号、双 ABI 构建、签名、GitHub/ModelScope 发布及发布后验收流程。
- 当前测试版本仍为 `26.8.28+3`，未因为本轮修复递增版本号。
- 重新构建并核验 v26.8.28.3：`dist/PiliExo_android_v26.8.28.3_arm64-v8a.apk`（26,604,031 bytes，SHA-256 `426BE600CCA5AD02C740063BE2315984ED74C178A6665AAE580A963AA108A1A8`）；`dist/PiliExo_android_v26.8.28.3_armeabi-v7a.apk`（26,479,661 bytes，SHA-256 `8590D4F9A77C13EBB5ED77B7CA6B68904DC3E1ACAD8A7DCEFDCAEF40C4947431`）。
- `flutter test --no-pub` 8/8、Flutter analyze（无 error/warning）、Android 单测和 Gradle Release 构建通过；未通过 ADB 安装或启动，v26.8.28.3 尚未发布。

## 2026-08-28 20:39

- 根据用户反馈移除主底栏和浮动底栏磨砂容器的顶部边框，保留磨砂材质，避免底栏上沿出现突兀亮线。
- 用户提供 `D:\read\gradle-9.5.0-all.zip` 后，已复制到 Gradle wrapper 标准缓存目录并验证 `Gradle 9.5.0` 可用，后续其它项目可复用该缓存。
- Flutter 分包命令遇到插件 Kotlin 增量缓存跨 C/D 盘符问题；使用 `kotlin.incremental=false` 和 `split-per-abi=true` 的 Gradle 9.5 任务重试成功。
- 测试版本仍为 `26.8.28+3`，未递增版本号。新双 ABI 成品：`dist/PiliExo_android_v26.8.28.3_arm64-v8a.apk`（26,603,903 bytes，SHA-256 `2B13DAA38924C02708549A61F09AFF7AA2844425AD8BC79C994D6E9DE390E686`）；`dist/PiliExo_android_v26.8.28.3_armeabi-v7a.apk`（26,479,041 bytes，SHA-256 `45C3AC59CB541BE496CC85715A5D8342744308A667019F2EBD3F941D92E7E283`）。
- `flutter test --no-pub` 8/8 通过，Gradle 9.5 Release 分包构建成功；未通过 ADB 安装或启动，v26.8.28.3 尚未发布。

## 2026-08-28 21:11

- 明确正式版本规则：测试包不递增版本号，正式发布编号取远端最新 Release 的 N+1；远端最新为 `v26.8.28.1`，本次正式发布为 `v26.8.28.2`。
- 已将提交 `1669ff368df0c12a72e92fe409c3b84737c925d5` 推送到 fork 的 `main`，创建并推送 `v26.8.28.2` 标签。
- GitHub Release 已发布：`https://github.com/maxzrb/PiliExo/releases/tag/v26.8.28.2`；ModelScope 数据集已同步到 `releases/v26.8.28.2/`，两个地址均实测 HTTP 200。
- 正式 APK：`dist/PiliExo_android_v26.8.28.2_arm64-v8a.apk`（26,603,631 bytes，SHA-256 `05E64EB063878F55DEE512B533BEB2BE1CCC6A39DC9CBB35A163DCA7A1FA027E`）；`dist/PiliExo_android_v26.8.28.2_armeabi-v7a.apk`（26,479,433 bytes，SHA-256 `05674F4765F134944B4E1B1DA3B63B192C2D85C025808D1BCBAFBC9E499EB0D1`）。
- APK 已核验包名 `com.maxzrb.piliexo`、versionCode `2` 和对应 native ABI；`flutter test --no-pub` 8/8、Gradle 9.5 Release 分包构建通过。
- 本次 APK 因未配置 `android/key.properties` 使用 debug keystore 兜底签名，适合自用测试；未通过 ADB 安装或启动，远程发布已完成。

## 2026-08-28 23:07

- 纠正版本元数据：远端最新正式 Release 仍是 `v26.8.28.2`，此前 `v26.8.28.3` APK 仅是测试构建，没有远端正式标签或 Release；`pubspec.yaml`、Dart 默认值和本机 `android/local.properties` 均恢复为 `26.8.28+2`，后续测试包不递增版本号。
- 从 PiliPlus 同步 3 个上游提交：`37ae9cf2d`、`4da811080`、`9058ac144`，本地对应 cherry-pick 提交为 `5935b03c1`、`032847b1b`、`b4bed5166`。
- 新增基于 AndroidX Media 1.11.0 `decoder_ffmpeg` 和 FFmpeg 6.0 的本地 AAR，启用 AC-3、E-AC-3/E-AC-3-JOC 核心、Dolby TrueHD；Media3 优先硬解，硬件不支持时使用 FFmpeg 软件回退，E-AC-3-JOC 软件路径保证 PCM 出声但不保证 Atmos 对象直通。
- Android 单测、R8 release 和 arm64/v7a 分 ABI 构建通过；新测试 APK 均为 `com.maxzrb.piliexo`、`26.8.28+2`，各自仅含对应 ABI 和 `libffmpegJNI.so`，未通过 ADB 安装或启动。
- 应用描述统一为“支持HDR的PiliPlus修改版”，发布流程补充了版本排错、测试冻结和 FFmpeg 扩展验收要点。
- 本轮不发布新 Release；手机顶部状态栏跟随视频模糊/变色和播放洞察按计划留待后续实现。
- 功能源码与 AAR 已提交为 `035314a79`（`fix: restore test version metadata and add Dolby fallback`）；本次最终交接记录补充提交为 `dbfbb1ecf`，本地 `pili_release.json` 已刷新到功能提交。

## 2026-08-29 00:09

- 按用户澄清，需求是手机顶部系统状态栏跟随视频的模糊背景，不是视频或播放器画面的模糊过渡；已参考官方 BiliPai 的真实实现：低分辨率当前帧采样、播放中约 66ms 更新、状态栏顶部独立背景和模糊遮罩。
- HDR 通过 Media3 `SurfaceView` 的 Android `PixelCopy` 采样 96×54 当前帧，SDR 通过现有视频区域 RepaintBoundary 采样；状态栏效果只占用顶部系统状态栏 inset，不改变 HDR SurfaceView 播放链路。
- 播放器洞察已接入视频页“更多设置 → 播放器洞察”，展示概览、视频、音频、播放、事件五类信息，并同时支持 Media3 HDR 和 mpv 已报告的格式、解码器、色彩、缓冲、首帧及掉帧数据。
- 测试版本保持 `26.8.28+2`，未创建新 Release 或标签。`flutter test --no-pub` 8/8、Flutter analyze 无 error、Android Kotlin 编译/单测、Gradle 9.5 R8 双 ABI Release 和 `aapt2` 包信息核验通过；本轮未通过 ADB 安装或启动。
- 测试 APK：`build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（26,930,502 bytes，SHA-256 `D5A1FCEBBEA942A7A014BAAB5D7A2BB6377645800AF86B28EC935DBB5A16B42A`）；`app-armeabi-v7a-release.apk`（26,808,350 bytes，SHA-256 `C4593E3357539BFE0CA454B1681C48DBE9747276B0F55DC4F54FBFC50D409183`）。

## 2026-08-29 00:35

- 中部播放器上滑/下滑切换全屏时，在越过阈值并实际执行全屏切换的瞬间加入震动反馈，复用设置里的开关和 1–255 强度。
- 修正播放器洞察入口：视频页“更多设置”现在固定显示“播放器洞察”，并放到菜单靠前位置；设置页的“播放器设置”中新增“显示 / 智能 / 不显示”三档选择。
- 智能模式按需求调整为：检测到新增掉帧后显示洞察摘要 5 秒，再用 350ms 渐隐消失；期间再次掉帧会重新计时。显示模式常驻，不显示模式关闭自动摘要，但仍可手动打开详情。
- 默认洞察模式为“智能”，测试版本保持 `26.8.28+2`，没有创建新 Release 或标签。
- `flutter test --no-pub` 8/8、Flutter analyze 无 error、Gradle 9.5 R8 双 ABI Release 构建和 `aapt2` 核验通过；本轮未通过 ADB 安装或启动。
- 新测试包：`build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（26,932,238 bytes，SHA-256 `3FC08D7B22227B034F265D5AD67B48357731BD28AD494DBE029D2CB2E0DBBD28`）；`app-armeabi-v7a-release.apk`（26,802,938 bytes，SHA-256 `1AB745AE2A5F235578BCB58090FE4412A6BBEE1FA65E02A020756EF5FA09E849`）。

## 2026-08-29 00:57

- 参考 BiliPai 的播放器同层详情交互，将洞察摘要点击行为改为在播放器内直接展开右侧详情面板；展开时显示半透明遮罩，点击遮罩、关闭图标或关闭按钮收起，不再由摘要点击弹出二级对话框。
- 洞察摘要下移并增加右侧内缩；详情面板按播放器实际尺寸自适应，横屏最大宽度约为可用宽度的 42%、最大高度 360，竖屏最大高度 520。
- 将 `PlaybackInsightHud` 放到播放器控件绘制层之后，避免详情展开时被底部控制条遮挡或抢占点击；“更多设置 → 播放器洞察”的手动入口保留原对话框形式。
- 测试版本仍为 `26.8.28+2`，未创建新标签或 Release。Flutter 单测 8/8、Gradle 9.5 双 ABI Release、`aapt2` 包信息核验通过。
- 测试 APK：`build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（26,937,774 bytes，SHA-256 `CEB32DA13C25A00A3B7DD497B7BA9E545A7BA8C8F24A8B76AEDD0D5292B73B46`）；`app-armeabi-v7a-release.apk`（26,807,498 bytes，SHA-256 `FCDC791140051CBB431FF0097C403B9E06589BEC0224A7FD2E7E797C3963FD46`）。

## 2026-08-29 01:09

- 重新按 BiliPai `VideoPlayerOverlay.kt` 的 `showInsightDetails` 逻辑实现播放器洞察：摘要所在的黑色半透明 surface 点击后直接扩展为播放器内的详情层；详情数据直接显示在扩展后的黑色 surface 中，取消独立右侧卡片视觉，点击遮罩、关闭图标或关闭按钮收起。
- 智能模式在起播首次获得有效播放器数据时显示 5 秒；新增掉帧仍显示 5 秒并重新计时；无数据时重置起播状态，保证下一段视频仍会显示起播洞察。
- 测试版本仍为 `26.8.28+2`，未创建新标签或 Release。Flutter 单测 8/8、Flutter analyze 无 error、Gradle 9.5 双 ABI Release、`aapt2` 包信息核验通过。
- 测试 APK：`build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（26,935,290 bytes，SHA-256 `5C3BD4F840C4442ECDDC2EDB96672ADE1D360D29FEC6C0564B00028EA77B73A7`）；`app-armeabi-v7a-release.apk`（26,810,654 bytes，SHA-256 `B1B2F9639ECAEF5C40A99DB10A4D880DE4B96A7C2A152DFE696744A9F32688FC`）。

## 2026-08-29 01:19

- 智能播放器洞察接入控制条状态：呼出进度条/控制条时主动显示，控制条关闭或自动隐藏时立即同步隐藏并收起详情。
- 起播首次拿到有效播放器数据显示 5 秒、新增掉帧显示 5 秒并重新计时的逻辑继续保留；控制条显示时不会被计时器提前隐藏。
- 测试版本仍为 `26.8.28+2`，未创建新标签或 Release。Flutter 单测 8/8、Flutter analyze 无 error、Gradle 9.5 双 ABI Release 和 `aapt2` 包信息核验通过。
- 测试 APK：`build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（26,939,058 bytes，SHA-256 `05B3D22D020EF57558E0BE9BC7FE0C223A3C97C6FB3D69E1B348641FB138901F`）；`app-armeabi-v7a-release.apk`（26,807,238 bytes，SHA-256 `788BD377A987C292BBC655E8E36825183461F564385527E550A7B14278D67A8B`）。

## 2026-08-29 01:28

- 根据用户反馈修正智能模式的可见性边界：控制条/进度条呼出时主动显示，收起时只清除控制条触发的显示并收起由控制条打开的详情；起播和新增掉帧触发的 5 秒事件窗口不被控制条收起取消，因此即使控制条已经隐藏，提示仍能正常出现。
- 测试版本仍为 `26.8.28+2`，未创建新标签或 Release。Flutter 单测 8/8、Flutter analyze 无 error（42 条既有 info）、Gradle 9.5 双 ABI Release 和 `aapt2` 包信息核验通过。
- 测试 APK：`build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（26,938,810 bytes，SHA-256 `B68176B0BFB53EF77AE49FFE708BB3D9233EF5529D7CF1FBA9C05DF40B97C95F`）；`app-armeabi-v7a-release.apk`（26,807,554 bytes，SHA-256 `A35D54C62FA438441F9273AB5C6D5A3CBA7B4063160D9A0146F261C62B6A5BE9`）。

## 2026-08-29 01:34

- 根据用户进一步澄清调整智能模式：未呼出控制条时，起播和新增掉帧事件独立显示 5 秒；控制条呼出后由控制条状态优先接管并覆盖当前事件窗口，控制条关闭时同步关闭洞察、收起详情并取消这次已被覆盖的事件窗口，避免自动提示继续遮挡重要画面。
- 检查 PiliPlus 上游 `main` 至 `9058ac144`：`37ae9cf2d`、`4da811080`、`9058ac144` 三个提交在本地已有等价 cherry-pick（分别为 `5935b03c1`、`032847b1b`、`b4bed5166`），没有新的上游提交需要合并。
- 测试版本仍为 `26.8.28+2`，本轮尚未重新构建新增逻辑，未创建新标签或 Release。

## 2026-08-29 01:38

- 完成智能洞察最终交互：未呼出控制条时，起播/掉帧事件各自显示 5 秒；控制条呼出后优先接管并覆盖当前事件窗口，控制条关闭时同步关闭洞察、收起详情并取消这次已被覆盖的事件窗口，避免重要画面继续被遮挡。
- 全屏洞察摘要再向左移动 12dp，右侧内缩从 24dp 调整为 36dp；普通窗口位置不变。
- 复查 PiliPlus 上游 `main` 至 `9058ac144`：三个上游提交均已在本地以等价 cherry-pick 存在，没有新的独有提交需要合并。
- 测试版本仍为 `26.8.28+2`，未创建新标签或 Release。Flutter 单测 8/8、Flutter analyze 无 error（42 条既有 info）、Gradle 9.5 双 ABI Release 和 `aapt2` 核验通过。
- 测试 APK：`build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（26,939,018 bytes，SHA-256 `DB7EDEE5882ACB802040E2A48A75C197223AC089011A8A3E9AA167E792FEA49A`）；`app-armeabi-v7a-release.apk`（26,807,566 bytes，SHA-256 `78D6B747E6CF5D9C90939F661B882E973E089532A3A71DB1499374E1B062223A`）。

## 2026-08-29 01:58

- 按日期版本规则完成正式发布：远端最新 `v26.8.28.2`，本次发布日期为 2026-08-29，因此发布 `v26.8.29.1`，应用版本 `26.8.29+1`；发布提交为 `e29dc3dc987248e6cee5a6d4acd13cd656d98bbc`。
- GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.1>；arm64 SHA-256 `65845C055CA83D7354AA498B0E567F457BEB91AFC1AABC43BAD453BAEDFD8324`，v7a SHA-256 `97C08218F9C257F473E04FD2E773F02D2FD65D551E9588338074A7EEBBCACF40`。
- ModelScope 镜像已上传：<https://modelscope.cn/datasets/AerithDream/PiliExo/resolve/master/releases/v26.8.29.1/PiliExo_android_v26.8.29.1_arm64-v8a.apk>、<https://modelscope.cn/datasets/AerithDream/PiliExo/resolve/master/releases/v26.8.29.1/PiliExo_android_v26.8.29.1_armeabi-v7a.apk>；两个地址 HTTP 200，`X-Linked-ETag` 与本地 SHA-256 一致。
- `flutter test --no-pub` 8/8、Android `:app:testDebugUnitTest -Pkotlin.incremental=false`、Gradle 9.5 双 ABI Release、`aapt2` 和 `apksigner` 核验通过；`flutter analyze --no-pub` 无 error，仅有 42 条既有 info。
- 因没有 `android/key.properties`，本次 APK 使用本机 Android Debug keystore；与上一正式包证书一致，可覆盖更新，但后续应配置固定正式签名。

## 2026-08-29 02:08

- 已生成固定正式签名 `android/piliexo-release.jks`：PKCS12、RSA 4096、有效期 10000 天；`android/key.properties` 已接入 Gradle，实际密钥与密码均未提交。
- 用正式签名重建并覆盖 `v26.8.29.1` 双 ABI APK：arm64 `27,003,414` bytes，SHA-256 `BE8198C3B071EDF7A8E25E9E872996BDCEAA6F9066DFF37EED88F53722B67166`；v7a `26,869,934` bytes，SHA-256 `07A58BBBA2B9D557C375E80B6D21A14DF4ADC3195E7FEF25D8E72AC7C98804E4`。
- 正式证书 SHA-256 为 `43f72e53fa2eaf3bb6a689573659217064df8066a1987822d94c7f109fa0e982`；同版本此前的 Debug 签名包无法覆盖安装，首次迁移需卸载一次，后续版本固定使用此密钥。

## 2026-08-29 11:01

- 修复播放器洞察查看详情时跟随智能模式隐藏逻辑被收起的问题；控制条显示状态变化不再关闭已打开的详情。
- 调整全屏播放器洞察详情窗口：沿用简要洞察的实际宽度、右侧位置和顶部位置，只向下增加纵向空间；普通窗口布局保持不变。
- 修改文件：`lib/plugin/pl_player/widgets/playback_insight.dart`。
- 验证通过：目标文件 Flutter analyze 无问题，全量 `flutter test --no-pub` 8/8 通过，`git diff --check` 通过；本轮未生成 APK，未进行真机验收。
- 当前工作树保留用户未跟踪目录 `tmp/`，本轮代码和记录改动尚未提交；测试继续使用版本 `26.8.29+1`，未创建新 Release。

## 2026-08-29 11:13

- 修复底栏点击当前“首页”或“动态”触发刷新时没有交互反馈的问题：现在会先显示对应页面原有的下拉刷新动画，再执行原有刷新请求。
- 首页推荐、热门、分区、直播、番剧、影视 tab 和动态各 tab 均支持程序化显示刷新动画；手动下拉刷新不变，页面未在顶部时仍先回到顶部。
- 修改通用刷新 mixin、底栏刷新调用、首页/动态刷新控件 key；未递增版本号、未创建新 Release。
- 验证通过：11 个相关 Dart 文件 Flutter analyze 无问题，全量 `flutter test --no-pub` 8/8 通过，`git diff --check` 通过；本轮未生成 APK，未进行真机验收。
- 当前工作树同时保留上一轮播放器洞察修复、本轮刷新动画修复及用户未跟踪目录 `tmp/`，尚未提交。

## 2026-08-29 11:30

- 修复播放器洞察详情缺少视频码率的问题：将已有的视频码率数据加入“视频”详情分组，音频码率继续显示在“音频”分组。
- 新增码率展示回归测试，防止视频码率字段再次遗漏。
- 验证通过：相关文件 Flutter analyze 无问题，全量 `flutter test --no-pub` 9/9 通过，`git diff --check` 通过；本轮未生成 APK，未进行真机验收。
- 当前工作树保留此前播放器洞察、底栏刷新动画及本轮码率修复改动，还有用户未跟踪目录 `tmp/`，尚未提交。

## 2026-08-29 12:06

- 按日期版本规则发布下一正式版 `v26.8.29.2`，应用版本更新为 `26.8.29+2`；发布提交 `68b9a377f9dc13aabd99d265e67bcaec81f11207` 已推送至 `maxzrb/PiliExo` 的 `main`，并创建正式标签。
- GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.2>；已上传 `PiliExo_android_v26.8.29.2_arm64-v8a.apk` 和 `PiliExo_android_v26.8.29.2_armeabi-v7a.apk`。
- ModelScope 数据集 `AerithDream/PiliExo` 已上传：<https://modelscope.cn/datasets/AerithDream/PiliExo/resolve/master/releases/v26.8.29.2/PiliExo_android_v26.8.29.2_arm64-v8a.apk>、<https://modelscope.cn/datasets/AerithDream/PiliExo/resolve/master/releases/v26.8.29.2/PiliExo_android_v26.8.29.2_armeabi-v7a.apk>。
- arm64 APK：`25,681,072` bytes，SHA-256 `E3988B841C173F8D3D2595299A639FC2EE2C1541CE1F80665832F3FB0A83BF92`；armeabi-v7a APK：`25,550,590` bytes，SHA-256 `D0876E6967C7DC5F182925E8083C4B5F2E62E0A92E1500715C018904489556E1`。
- 发布验证通过：Flutter 单测 10/10、定向 Dart analyze、`git diff --check`、`aapt2` 包名/版本/ABI/`libffmpegJNI.so` 核验、`apksigner` V2 正式签名核验；ModelScope 回读文件 SHA-256 与本地产物一致。
- 构建使用本机 Flutter 3.47.1（项目声明 3.47.2），通过 `kotlin.incremental=false` 处理跨盘 Kotlin 缓存问题；未进行 ADB 真机安装或启动验收。用户未跟踪目录 `tmp/` 继续保留且未提交。

## 2026-08-29 12:49

- 修复播放器洞察视频码率数据链路：DASH 视频/音频轨道码率写入 `DataSource`，Media3 HDR、mpv 和 HDR 失败回退在原生轨道未返回码率时使用数据源回退值；“概览”和“视频”详情分组均显示“视频码率”。
- 全屏播放器洞察参考 BiliPai 的覆盖层分档：摘要按屏幕宽度调整顶部/右侧位置、字体和最小高度，详情固定在右侧并限制约 42% 宽（320–440dp）及 360dp 高；普通窗口保持原布局。
- Android 检查更新接入 Aria2-next 2.6.7 ARM64 二进制，配置 32 分片、32 连接上限、断点续传、完整性检查和 Release SHA-256 校验；成功后自动唤起系统 APK 安装器。非 ARM64 或下载器不可用时回退应用内 HTTP 下载。
- 新增 Android 更新 MethodChannel、FileProvider、安装权限、更新文件路径校验和 Aria2-next 许可证说明；标准 Android 安装确认仍由系统处理。
- 验证通过：`flutter test --no-pub` 11/11；相关 Dart 文件 `flutter analyze --no-pub --no-fatal-infos` 无 error（保留控制器原有 4 条 info）；Android `:app:compileDebugKotlin -Pkotlin.incremental=false`、`assembleDebug -Pkotlin.incremental=false`、APK 资源/Manifest 核验和 `git diff --check` 均通过。
- 当前版本仍为 `26.8.29+2`，本轮未创建新 Release；未进行 ADB 真机安装或启动验收。工作区保留用户未跟踪目录 `tmp/`，功能改动尚未提交。

## 2026-08-29 13:24

- 统一编译工具链：本机准备并验证 Flutter `3.47.2` SDK；`.fvmrc`、`pubspec.yaml`、`pubspec.lock` 和实际 SDK 均通过 `lib/scripts/verify_flutter.ps1` 校验，5 个 GitHub Actions 工作流改为从 `.fvmrc` 读取版本并在构建前校验。
- 修复 Android/Kotlin 增量缓存跨盘问题：`android/gradle.properties` 固定 `kotlin.incremental=false`，本地和 CI 不再需要追加 `-Pkotlin.incremental=false`；保留 `android.builtInKotlin=false`、`android.newDsl=false`，因为当前 Flutter Gradle 插件与 AGP 9 新 DSL 直接组合会触发类型冲突。
- 发布流程文档已补充固定工具链校验、跨盘构建约束和标准检查命令；本轮不递增 `26.8.29+2`，未创建新 Release。
- 验证通过：Flutter 工具链校验、`flutter test --no-pub` 11/11、全量 `flutter analyze --no-pub --no-fatal-infos` 无 error、默认 `android\gradlew.bat :app:compileDebugKotlin --console=plain`、标准 `flutter build apk --debug --no-pub` 和 `git diff --check`。
- Debug APK：`build/app/outputs/flutter-apk/app-debug.apk`，208,644,128 bytes，SHA-256 `EC4C6A6DAD3FDC5640E57F0E11A1229A38D96AAC707A1F4AB54EFE5F2793E3F7`；已核验包含 Aria2-next ARM64 资源、许可证说明和 FileProvider。
- 标准 ARM 双 ABI Release 构建成功：arm64 `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（31,662,862 bytes，SHA-256 `72E7F5FE88BAFA2CD07EE83D6CDE30B49FD6556607590753FBF70BD6F72E21BA`），v7a `build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk`（31,536,514 bytes，SHA-256 `923102E4C35B626B0B9F5C5448274506B436D6AAE927CEAE2DFA9EB6F201D5D9`）；`aapt2` 核验包名 `com.maxzrb.piliexo`、`versionName=26.8.29`、`versionCode=2` 和对应 ABI，`apksigner` V2 验证通过。
- 当前工作区保留用户未跟踪目录 `tmp/`，本轮源码、工作流和记录改动尚未提交；未进行 ADB 真机安装或启动验收。

## 2026-08-29 13:53

- 按同日正式发布规则将版本递增为 `v26.8.29.3`，应用版本为 `26.8.29+3`；发布提交 `4b3baa808180c40c6857b71f0701ee70e4bc3ec5` 已推送至 `maxzrb/PiliExo` 的 `main`，并创建、推送带注释标签。
- GitHub Release 已发布：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.3>；arm64 与 armeabi-v7a 两个资产均已上传。
- ModelScope `AerithDream/PiliExo` 已同步至 `releases/v26.8.29.3/`；两个镜像地址 HTTP 200，`X-Linked-ETag` 与本地 SHA-256 一致。
- arm64 APK：31,663,011 bytes，SHA-256 `F127645D45EB69E74616FFD4937BA9A9D2EFD819842CA1FE4C066B3770B71224`；armeabi-v7a APK：31,536,321 bytes，SHA-256 `D9D871CC1C7F0C6EB6E0ABC42ABE301F6C5FDB7CCF818B1B65F7FA3D58136565`。两个包均通过 `aapt2` 和 `apksigner` V2 核验。
- 发布前验证通过：Flutter 3.47.2 工具链校验、`flutter pub get`、`flutter test --no-pub` 11/11、全量 analyze 无 error、标准 ARM 双 ABI Release 构建和 `git diff --check`。
- 当前工作区仅保留用户未跟踪目录 `tmp/`；本轮发布收尾记录已写入，准备提交，不修改或提交该目录。

## 2026-08-29 14:06

- 根据用户反馈修正播放时画质/音质切换：播放器底栏和视频页顶部控件不再回写“默认画质”“移动数据画质”“默认音质”“移动数据音质”，只更新当前播放器。
- 设置页四个默认画质/音质选项的手动保存逻辑保持不变；“播放器设置仅对当前生效”仍继续控制其它播放器设置。
- 修改文件：`lib/plugin/pl_player/view/view.dart`、`lib/pages/video/widgets/header_control.dart`。
- 验证通过：两个目标文件 Flutter analyze 无问题，`flutter test --no-pub` 11/11 通过，`git diff --check` 通过；本轮未打包、未创建新 Release。
- 当前版本仍为 `v26.8.29.3` / `26.8.29+3`；工作区保留用户未跟踪目录 `tmp/`，本轮代码与记录改动尚未提交。

## 2026-08-29 15:28

- 已在回退基线 `670de9d07`（`fix: prevent playback quality changes from updating defaults`）之上接入可选 Android 液态玻璃，保留该提交作为前序改动的兜底点；工作区未把用户未跟踪目录 `tmp/` 纳入版本控制。
- 新增“Android 液态玻璃”开关，默认关闭，仅 Android 13（API 33）及以上可用，独立于磨砂半透明设置并即时生效；关闭、不支持或运行时熔断时继续使用原有磨砂/普通背景。
- 使用 Kyant Backdrop `2.0.0` 和 Compose PlatformView 覆盖浮动/固定底栏、首页搜索胶囊及非播放器页面主悬浮按钮；原生层局部取样 FlutterImageView，最高约 30 FPS，窗口失焦、应用暂停或连续取样失败时停止/回退。
- 原生视图同步选中索引、主题色和按压状态；Flutter 继续处理导航、点击、刷新、震动、文字和无障碍语义；播放器控制层、播放器洞察、HDR SurfaceView 不接入液态玻璃。
- 新增 `AndroidLiquidGlassPluginTest` 和 `liquid_glass_surface_test`，补充参数、坐标、生命周期、失败熔断、API 门槛、默认值和持久化回归覆盖；加入 `android/app/src/main/assets/licenses/kyant-backdrop-Apache-2.0.txt`。
- 验证通过：Flutter 全量测试 13/13、目标文件 analyze 无问题、Android 原生单测通过、默认 `compileDebugKotlin` 通过、Debug `assembleDebug` 通过、`git diff --check` 通过。
- 最终 Debug APK：`build/app/outputs/flutter-apk/app-debug.apk`，`160,141,934` bytes，SHA-256 `D7E2069C96ECD45191472B45F9EBA4E9EBDB12B1500041C28760E40045B48860`；APK 内已核验 Kyant Apache-2.0 许可资产。
- 本轮保持版本 `v26.8.29.3` / `26.8.29+3`，不打包发布 Release；仅生成开发 Debug APK，尝试安装时被设备拒绝 USB 安装权限，尚未完成 ADB 真机和 Android 13/14/15 性能门槛验收。

## 2026-08-29 15:45

- 修复 Android `BiliDocumentsProvider` authority 硬编码问题：改为 `${applicationId}.MTDataFilesProvider`，Debug 使用 `com.maxzrb.piliexo.debug.MTDataFilesProvider`，Release 使用 `com.maxzrb.piliexo.MTDataFilesProvider`，可与已安装的另一构建变体并存。
- 使用固定 Flutter `3.47.2` SDK 重新执行 `flutter build apk --debug --no-pub`，构建成功；合并 Manifest 已核验 Debug authority 正确生成。
- ADB 安装验证已不再出现 `INSTALL_FAILED_CONFLICTING_PROVIDER`，设备当前返回 `INSTALL_FAILED_ABORTED: User rejected permissions`，需要在设备端确认 USB 安装授权。
- 当前版本仍为 `v26.8.29.3` / `26.8.29+3`，未创建新 Release；工作区保留用户未跟踪目录 `tmp/`，源码和记录改动尚未提交。

## 2026-08-29 16:05

- 修复 Android 液态玻璃启用后的崩溃风险：每帧使用独立 Bitmap 快照并延迟回收旧快照，避免 Compose RenderThread 读取时发生并发改写/回收；每个控件只保留一层 Kyant `drawPlainBackdrop`，底栏选中态改为普通高亮层。
- 增加液态玻璃启动探测保护，并将配置初始化移动到 Hive 完成之后：升级后已有开启状态先安全回退；本次启用或上次启动异常会留下探测标记，稳定运行 15 秒后清理，异常下次启动不重复创建原生视图，用户保存的开关不被清除。
- 验证通过：Flutter 全量 `test --no-pub` 14/14、液态玻璃定向 analyze 无问题、Android 原生单测、默认 Kotlin 编译、Debug APK 构建和 `git diff --check`。
- 最终 Debug APK：`build/app/outputs/flutter-apk/app-debug.apk`，`214,432,947` bytes，SHA-256 `95FC380AD172C85F82EDB7AF9A4B29119F103DB2E1F59231AB918F9C8DE03A3A`。
- 设备端覆盖安装仍返回 `INSTALL_FAILED_ABORTED: User rejected permissions`，尚未完成最终包的真机启动/渲染验收；当前版本仍为 `v26.8.29.3` / `26.8.29+3`，未创建新 Release，源码和记录改动尚未提交。

## 2026-08-29 16:48

- 根据设备崩溃日志定位并修复液态玻璃的实际 Java 异常：ComposeView 挂载到 FlutterView 时找不到 `ViewTreeLifecycleOwner`，此前会在 Flutter 平台视图 JNI 检查处升级为 SIGABRT；现在由 `LiquidGlassViewTree` 在 Activity decor 根视图和 ComposeView 挂载前绑定 FlutterActivity 生命周期，宿主不兼容时创建透明回退视图。
- 补充 `androidx.lifecycle:lifecycle-runtime-android:2.9.4` 依赖，并继续保留 TLHC/Virtual Display 回退、SurfaceView 局部 PixelCopy、原生异常兜底和液态玻璃安全版本探测。
- 验证通过：Flutter 全量测试 14/14、液态玻璃定向 analyze、Android `:app:testDebugUnitTest`、Kotlin 编译、Debug APK 构建和 `git diff --check`。
- 最新 Debug APK：`build/app/outputs/flutter-apk/app-debug.apk`，`214,432,460` bytes，SHA-256 `41A9157489B9F214D822C386E47F53B7434CA64F44C3A413743B08B1DE3D9184`。
- 设备当前仍处于锁屏状态，安装最新包仍被系统返回 `INSTALL_FAILED_ABORTED: User rejected permissions`，因此尚未完成最新包的真机启动确认；不创建新 Release，工作区继续保留用户未跟踪目录 `tmp/`。

## 2026-08-29 17:02

- 设备已成功安装并启动上一版 Debug APK，确认 `ViewTreeLifecycleOwner` 问题已消失；新日志进一步暴露 ComposeView 要求 `ViewTreeSavedStateRegistryOwner`，因此新增与 FlutterActivity 生命周期同步的轻量 SavedStateOwner，并将其安装到 FlutterView、Activity 根视图和 ComposeView。
- 原生视图树兼容代码改为 Java 辅助层，显式覆盖 FlutterView 根节点；新增 `androidx.savedstate:savedstate-android:1.4.0` 依赖。Android 原生单测、Kotlin 编译和 Debug APK 构建均通过。
- 最新 Debug APK：`build/app/outputs/flutter-apk/app-debug.apk`，`214,432,460` bytes，SHA-256 `032F54C849001705F1590DB5E25C7FD9DF4F06160CCAF8D1344F8C0823BE3B7E`。
- 当前设备上的 16:53 包仍是上一构建，最新 17:02 包尚未完成设备安装确认；不创建新 Release，工作区继续保留用户未跟踪目录 `tmp/`。

## 2026-08-29 17:04

- 增加液态玻璃平台视图挂载后的二次 ViewTree owner 绑定：覆盖创建时 FlutterView 尚未进入 Activity 视图树的时序，避免首次 measure 时再次缺少 Lifecycle/SavedState owner。
- 验证通过：Android `:app:testDebugUnitTest`、Kotlin 编译、Debug APK 构建和 `git diff --check`；最新 Debug APK：`build/app/outputs/flutter-apk/app-debug.apk`，`214,432,460` bytes，SHA-256 `276BEEE7CFAAC3DD7DB7EFB0B3DFB44024C9EF36C43001041CB24B8DF1EF9D75`。
- 设备端覆盖安装仍返回 `INSTALL_FAILED_ABORTED: User rejected permissions`，最新包尚未完成真机启动确认；不创建新 Release，工作区继续保留用户未跟踪目录 `tmp/`。

## 2026-08-29 17:28

- 参考 BiliPai 和 Kyant 官方示例恢复液态玻璃真实材质层：底栏外壳改用 `drawBackdrop`，补回活力、模糊、lens、边缘高光、阴影和内阴影；选中胶囊使用独立的 lens、深度折射与色散，并降低按压缩放幅度。
- Flutter 背板取样从控件矩形扩大为带 32dp（搜索/FAB 按预设缩小）边缘余量的窗口，保存目标在快照中的 inset；Compose 端按真实像素坐标绘制，避免局部截图缩放导致折射内容失真。
- 验证通过：Android `:app:testDebugUnitTest`、Kotlin 编译、Debug `assembleDebug`、目标 Dart 文件 analyze；Debug APK SHA-256 为 `DB484BEC24F9CC0D7637541671E6374ADBF406D768C37C36DE9D4C14DC4470DC`。
- 设备处于锁屏/通知遮罩，`adb install -r -d` 仍返回 `INSTALL_FAILED_ABORTED: User rejected permissions`，本轮未完成真机视觉验收；当前版本不变、不发布 Release，代码尚未提交。

## 2026-08-29 17:37

- 按要求停止 Android 液态玻璃方案并完成回退，移除 Kyant/Compose 依赖、原生取样 PlatformView、Flutter 包装、设置项、许可证和相关测试。
- 底栏、首页搜索和普通页面悬浮按钮已恢复原有渲染；此前播放器洞察、视频码率、刷新动画、默认画质/音质设置和 Provider 冲突修复均保留。
- 验证通过：Flutter 单测 11/11、全量 analyze 无 error、Android Kotlin 编译、`git diff --check`。
- 当前仍为 `v26.8.29.3`，本轮不打包、不发布 Release；用户未跟踪的 `tmp/` 目录保留，代码尚未提交。

## 2026-08-29 18:11

- 修复播放器洞察摘要点击不稳定：扩大摘要点击层到整个黑色 surface，且智能摘要开始渐隐后的 350ms 内仍可点击进入详情。
- 新增摘要留白区域和渐隐期间的交互回归测试；Provider authority 修复继续保留。
- 已准备正式版本 `v26.8.29.4` / `26.8.29+4`，接下来执行双 ABI 构建与 Release 发布。

## 2026-08-29 18:19

- 已发布正式版 `v26.8.29.4`，发布提交 `ae6c0fcc286a2a99cd97b403a5b0a1dedea555ff` 已推送至 `maxzrb/PiliExo` 的 `main` 并创建标签。
- GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.4>；ModelScope 双 ABI 镜像已上传并通过 HTTP 200、文件长度和 SHA-256 回读校验。
- arm64 APK：31,662,286 bytes，SHA-256 `2F93405EC16B18C054DD65899754FDD193AAF7F995EFF610EF1CF334407A5110`；armeabi-v7a APK：31,536,417 bytes，SHA-256 `39CC232BE1BE933A2EC64586E4BF8E37325E8F58FF7B6D71F737B5328691F616`。
- 播放器洞察摘要留白点击、智能摘要渐隐期间点击均已修复并通过交互测试；Flutter 单测 12/12、Analyze 无 error、Android 双 ABI 正式构建和正式签名校验通过。

## 2026-08-29 18:46

- 更新下载现在沿用发现新版本的更新弹窗：点击下载后弹窗内显示“准备下载、Aria2-next 32 分片/普通 HTTP 下载、校验安装”等阶段，同时显示实时进度条和已下载/总大小。
- Android Aria2-next 通过 MethodChannel 按约 250ms 回传文件大小；非 ARM64 的 Dio 回退下载使用接收进度回调；Release 资产没有大小时仍显示不确定进度。
- 弹窗增加“取消下载”，可终止 Aria2 或 Dio 下载；取消和关闭弹窗都不会触发浏览器回退或安装不完整文件。
- 修改：`lib/utils/update.dart`、`android/app/src/main/kotlin/com/example/piliplus/AndroidUpdatePlugin.kt`、`test/utils/update_asset_test.dart`。
- 验证：Flutter 测试 14/14 通过，`flutter analyze --no-pub --no-fatal-infos` 无 error，Android `:app:assembleDebug` 成功，`git diff --check` 通过。
- 当前版本仍为 `v26.8.29.4` / `26.8.29+4`，本轮暂不发布新 Release；工作区保留用户未跟踪目录 `tmp/`，源码和记录尚未提交。

## 2026-08-29 19:00

- 按要求不递增版本，已将更新下载弹窗交互修复覆盖到正式版 `v26.8.29.4`；源码提交为 `7a2edd7c9fd1ee9aaaf71e5052301ed04b60c5e2`，同名标签已移动到该提交。
- GitHub Release 已替换同名双 ABI 资产并更新 Release Note：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.4>。
- ModelScope 同路径双 ABI 资产已覆盖，HTTP 200，`X-Linked-ETag` 与 GitHub digest、本地产物 SHA-256 一致。
- arm64 APK：`31,666,544` bytes，SHA-256 `0828E730F59173416761ECE5467907F625BD1B51487F502D7F7277BF6C1CA359`；armeabi-v7a APK：`31,542,223` bytes，SHA-256 `A54C25EE33934229554ADB1B012748AFF9E44CA05425D729A4243D09F4892197`。
- 发布包、正式签名、Flutter 测试 14/14 和 Android Release 构建均已核验通过；发布记录已提交并推送，用户未跟踪目录 `tmp/` 未作处理。

## 2026-08-29 19:34

- 以 `04f32f4ca`（v26.8.29.4 更新弹窗修复发布后的提交）为回退基线，建立本地回退分支 `backup/pre-upstream-font-sync-20260829`。
- 安全引入 PiliPlus 上游字体相关线性提交：字体页面大重构、默认字体回退、选择区域补丁、字体设置存储重构、依赖升级和字体设置保存修复；六个上游提交均无冲突落地。
- 由于字体存储从多文件索引改为单一 `customFont.otf`，补充旧版 `customAppFont` 索引迁移，复制当前用户字体并保留旧数据以支持回退；自定义字体保存等待写盘完成。
- 依赖升级将 `flutter_html` 切换为 Git `dev` 分支并锁定解析提交 `e33a789a4dce561267a995d3bc48c638c3950e02`，其余锁文件变更按上游保留。`flutter pub get` 已解析成功，但 Windows 未开启开发者模式导致插件符号链接检查返回非零，未额外改写锁文件。
- 验证通过：`flutter test --no-pub` 14/14，全量 Analyze 无 error（42 条既有 info），Android `compileDebugKotlin` 和 `assembleDebug` 均通过。
- 版本仍为 `26.8.29+4` / `v26.8.29.4`，未发布新 Release、未推送；源码和本记录已提交，用户未跟踪目录 `tmp/` 保留不变。

## 2026-08-29 19:45

- 按用户要求发布下一版 `v26.8.29.5`，应用版本为 `26.8.29+5`；发布提交 `7f4595a2e51c0b1303ea92fd8b12ad05be4426bb` 已推送到 `maxzrb/PiliExo` 的 `main`，标签已创建。
- GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.5>；双 ABI 资产已上传。ModelScope `AerithDream/PiliExo` 的 `releases/v26.8.29.5/` 双 ABI 路径已同步。
- arm64 APK：31,641,310 bytes，SHA-256 `0AFDF2C4F537BE138A3F75CDBDED051D9909DFA5165D096AD0B65670D0CAB3BA`；armeabi-v7a APK：31,514,507 bytes，SHA-256 `A2AC73058AA1118AFA495DDDCBE8A162A28715E4C66FD1267FAB73FB6E508533`。
- GitHub 资产和 ModelScope 镜像均已回读校验：HTTP 200、文件长度和 SHA-256 一致；包名、版本号、ABI、`libffmpegJNI.so` 和正式签名 V2 校验通过。
- 发布前验证通过：Flutter 3.47.2 工具链、`flutter pub get`、`flutter test --no-pub` 14/14、全量 Analyze 无 error、Android Debug/Release 构建和 `git diff --check`。
- 当前版本为 `v26.8.29.5` / `26.8.29+5`；用户未跟踪目录 `tmp/` 保留不变，未纳入提交。

## 2026-08-29 20:32

- 发布更新机制测试版本 `v26.8.29.6` / `26.8.29+6`，不增加额外功能改动，专门用于验证从 `v26.8.29.5` 检测更新、选择下载源、显示下载进度和启动安装流程。
- GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.6>；Release 为正式状态，双 ABI 资产已上传。ModelScope `AerithDream/PiliExo` 的 `releases/v26.8.29.6/` 双 ABI 路径已同步。
- 发布提交 `6ea847e367ee7c39e0d08239238a0f1ba25aefbe` 已推送到 `main`，标签已创建；双 ABI 包的包名、版本号、ABI、FFmpeg 资源、正式签名和 SHA-256 校验通过。
- arm64 APK：31,641,669 bytes，SHA-256 `229C4208DF487805B697E31A7D0BFC1C6EBDA09517711E5CE16B49D529DBC9B2`；armeabi-v7a APK：31,514,450 bytes，SHA-256 `7993E28E1B468C7CDE77ECA003CB10E80640183AC42464059688CE88C208FD62`。
- GitHub 与 ModelScope 回读均正常，HTTP 200、文件长度和 SHA-256 一致；Flutter 测试 15/15、Analyze 无 error、Android Debug/Release 构建均通过。
- 用户未跟踪目录 `tmp/` 保留不变，未纳入提交。

## 2026-08-29 20:23

- 修复更新弹窗下载进度位置：进度状态区移到滚动更新说明之外，下载时固定可见，无需滚动查看；更新说明仍可滚动浏览。
- 修复控制条/进度条隐藏后关闭播放器洞察详情时摘要闪现的问题，并新增 widget 回归测试。
- 按用户要求覆盖最新 `v26.8.29.5`：修复提交 `8f15d8547146d799ffa8d92138cdbea5e55b8623` 已推送，`v26.8.29.5` 标签已移动到该提交，GitHub Release 说明和双 ABI 资产已更新，ModelScope 同路径资产已覆盖。
- arm64 APK：31,641,652 bytes，SHA-256 `D3DE77320FDE29C988E215EADFC887C0F646B327283A5026BEE2E94AA3C85E7E`；armeabi-v7a APK：31,514,355 bytes，SHA-256 `271D167B11E1A3741D5A43686E376DDE4E36A94A7A0233729E938418FC849CCC`。
- 验证通过：Flutter 测试 15/15、全量 Analyze 无 error、Android Debug/双 ABI Release 构建、包名/版本/ABI/FFmpeg 资源/正式签名校验，GitHub 与 ModelScope 哈希一致。
- 版本号仍为 `26.8.29+5`，本轮仅覆盖同版本 Release；用户未跟踪目录 `tmp/` 保留不变。

## 2026-08-29 21:25

- 修复更新弹窗下载进度位置：更新日志独立滚动，进度状态区固定在更新日志下方、取消下载按钮上方，下载时无需滚动即可看到进度。
- 参考 BiliPai 首页推荐合并策略，新增 Web/App 推荐混合模式：并行请求两路推荐，按 App 占比交错合并并按 bvid/avid 去重；0% 和 100% 分别保留纯 Web、纯 App 请求路径，单路失败时用另一侧兜底。
- 推荐设置由旧版 App/Web 布尔开关改为 Web 推荐～App 推荐两端滑块，按 10% 步进调整 App 占比；旧版 `appRcmd` 设置自动兼容为 0% 或 100%，新值保存至 `appRcmdRatio`，仍保持重启生效。
- 新增 `lib/utils/recommend_mix.dart` 及 4 项推荐比例/混合去重测试，更新 `SliderDialog` 支持两端说明。
- 验证通过：固定 Flutter 3.47.2 下 `flutter test --no-pub` 19/19、全量 analyze 无 error（42 条既有 info）、`flutter build apk --debug --no-pub` 成功；Debug APK `154,168,037` bytes，SHA-256 `52DBB53B46813315C05DB198BBBB3906457AC8BE159794376F4018189555EFB7`，`git diff --check` 通过。
- v26.8.29.6 已完成同版本 Release 覆盖；用户未跟踪目录 tmp/ 保留不变，未纳入发布提交。

## 2026-08-29 21:48

- 将更新弹窗进度布局和 Web/App 推荐混合比例设置提交为 3e9e80949，补充发布记录修正提交 9dea9e7da、a576d369；提交已推送至远端 main。
- 按用户要求保持 26.8.29+6，将标签 v26.8.29.6 移动到 a576d369216a885fc31bd60dc65bd989c3275919，并覆盖同名 GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.6>。
- GitHub 双 ABI 资产已覆盖：arm64-v8a 31,646,652 bytes，SHA-256 5A456E6F0B0C62F1EA2DAF7C75C042DC9E5FC5DAC7AD7977B20709703B4C4C97；armeabi-v7a 31,519,008 bytes，SHA-256 2C1013E0BE56604D404AC08DABBDA5C41D02802AA2E6AE69A0D7B7E5A73553C5。
- ModelScope AerithDream/PiliExo 的 releases/v26.8.29.6/ 双 ABI 资产已覆盖；远端 HTTP 200、Content-Length 和 X-Linked-ETag 均与本地产物一致。
- 发布校验通过：固定 Flutter 3.47.2、flutter test --no-pub 19/19、全量 analyze 无 error（42 条既有 info）、Debug/双 ABI Release 构建、aapt2 包名/版本/ABI/FFmpeg 资源核验、两个 APK 的 apksigner V2 签名核验和 git diff --check。
- 本轮未递增版本、未改变播放器渲染后端，未处理用户未跟踪目录 tmp/。

## 2026-08-29 22:13

- 修复首页推荐来源设置状态：混合比例说明仅在设置刚变更且尚未刷新时显示“下次刷新生效”，首页刷新成功后自动清除，后续再次进入设置页只显示当前比例。
- RcmdController 改为每次推荐请求动态读取最新比例，设置保存后无需重启，下一次下拉刷新即可应用；新增待刷新状态存储键和回归断言。
- 发布提交 2381922ada34af2cc8cd674c166196314d81dd11 已推送至远端 main，并创建正式标签 v26.8.29.7。
- GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.29.7>；arm64-v8a APK 31,647,323 bytes，SHA-256 3C3C75CD209698B1C270401258F275FEEAA81ABD25E9658E384079554F767629；armeabi-v7a APK 31,519,676 bytes，SHA-256 D240C537DA0A0B317A4854E4E21556FAE86ED025D0D8F15FA31408ED6C9BFF2C。
- ModelScope AerithDream/PiliExo 的 releases/v26.8.29.7/ 双 ABI 资产已同步；两个地址 HTTP 200，Content-Length 和 X-Linked-ETag 与本地产物一致。
- 发布校验通过：Flutter 3.47.2、flutter test --no-pub 20/20、全量 analyze 无 error（42 条既有 info）、双 ABI Release 构建、aapt2 核验 versionCode=7/对应 ABI/FFmpeg 资源、两个 APK 的 apksigner V2 签名核验和 git diff --check。
- 本轮公开 Release 说明未加入额外用途描述；版本号为 26.8.29+7，播放器渲染后端未改变，用户未跟踪目录 tmp/ 未处理。

## 2026-08-29 22:39

- 重构根目录 `README.md`：顶部补充 PiliExo 的 Android 定位、HDR 播放器、播放器洞察、更新下载、推荐混合、默认画质/音质和字体重构等实际改动，以及当前构建与下载说明。
- 在“以下为原项目readme内容”分隔标记后保留 PiliPlus 上游 `main` README，来源基线为 `9624c37255837412e8677a6b9d69fdfac720b68a`；仅清理 3 处徽章链接尾随空白。
- 本轮未修改版本号、未构建或发布新 Release；`git diff --check` 通过，用户未跟踪目录 `tmp/` 未处理。

## 2026-08-30 00:42

- 从上游 `main` 同步 `9624c37255837412e8677a6b9d69fdfac720b68a` 和 `06b88943ca5fa787ff797dfa51af0147bee76ca9`；分别落为 `6540ae5e6`、`8ee4f83d6`，再合入发布提交 `31c000054bbfab05af9addac864c287f5dbe8469`。
- `9624c37` 在动态页和播放器控制器产生冲突：保留 PiliExo `FrostedPreferredSize`、动态页主题色和 Media3 HDR 画中画分支，同时吸收其余上游改动；`06b8894` 的日志信息复制修复和 `catcher_2` 锁定提交更新无冲突应用。
- 验证通过：Flutter 工具链 `3.47.2`、`flutter pub get`、`flutter test --no-pub` 20/20、全量 analyze 无 error（42 条既有 info）、Android Debug 构建、双 ABI Release 构建、`aapt2` 包名/版本/ABI/FFmpeg 资源校验、`apksigner` V2 正式签名校验和 `git diff --check`。
- `v26.8.30.1` APK：arm64-v8a `31,632,512` bytes，SHA-256 `320495B4189ABA65D98D5B310D35EE5B10164929FD52A561FD9E4E921BDA8A10`；armeabi-v7a `31,507,528` bytes，SHA-256 `1AF660424FD534A2413837AD02908D1C538B03E8202352188F53D3C458B1B05B`。
- 已推送 `main` 和标签 `v26.8.30.1`；GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.30.1>，双 ABI 资产均已上传。
- ModelScope `AerithDream/PiliExo` 已同步 `releases/v26.8.30.1/` 双 ABI 资产；两个地址 HTTP 200，Content-Length 与 X-Linked-ETag 均与本地产物一致。
- 本轮未执行真机交互回归；本地回退分支 `backup/pre-upstream-9624-20260830` 保留，用户未跟踪目录 `tmp/` 未处理。

## 2026-08-30 01:10

- 定位并修复 `v26.8.30.1` 无法覆盖 `v26.8.29.7` 的原因：前者错误地将日期发布序号 `1` 同时作为 Android `versionCode`，被系统判定为低版本。
- 新增 `android/version.properties` 作为全局 Android 版本号来源，本次 `v26.8.30.2` 使用 `versionCode=8`；Gradle 不再接受 `android/local.properties` 中过期的 `flutter.versionCode` 覆盖正式版本号。
- `pili_release.json` 分离 `pili.code`（Android `versionCode`）和 `pili.releaseBuild`（标签末段）；更新检查、关于页面和 Release 标签继续使用 `v26.8.30.2` / `26.8.30+2`。
- 发布流程已明确：日期变化时标签末段可从 1 重新开始，但每个新 Release 的 Android `versionCode` 必须在上一正式包基础上加 1；同名 Release 资产覆盖才可保持原值。
- 验证通过：Flutter 3.47.2 工具链、`flutter pub get`、`flutter test --no-pub` 21/21、全量 analyze 无 error（42 条既有 info）、Gradle Debug/双 ABI Release 构建、`aapt2` 包名/版本/ABI、`apksigner` V2 签名和 `git diff --check`。
- `v26.8.30.2` 发布提交 `bee12ddbddefca9527839629ba69f11ed932a0392` 已推送到 `maxzrb/PiliExo` 的 `main` 并创建正式标签；GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.30.2>。
- arm64-v8a APK：`31,630,867` bytes，SHA-256 `04C9B1AB3C078B8D585725333BD9C8E47575FF3BF2919FF987574037CE0448D5`；armeabi-v7a APK：`31,509,357` bytes，SHA-256 `39AB66AF59353FFA13DF4EAA7594D31211105C829B85306E447543CD11823A12`；两个包均包含对应 `libffmpegJNI.so`，正式签名 V2 校验通过。
- ModelScope `AerithDream/PiliExo` 已同步 `releases/v26.8.30.2/` 双 ABI 资产；两个地址 HTTP 200，Content-Length 和 X-Linked-ETag 与本地产物哈希一致。
- 本轮未执行真机交互回归；用户未跟踪目录 `tmp/` 保留不变，未纳入版本控制。

## 2026-08-30 10:18

- 修复 PiliExo 自动音轨选择：目标音质实际存在时继续优先使用目标；目标不存在时按 Hi-Res、Dolby Atmos（30250）、Dolby Audio（30255）、192K、132K、64K 的明确优先级选择实际可用音轨。
- 删除目标音质不存在且存在更高 ID 音轨时强制回退 192K 的逻辑，不再通过 audio ID 数值大小判断音质；下载、视频播放和音频播放三条路径统一使用 `AudioTrackSelector`。
- 保留 Media3 原生 Dolby 解码优先和 FFmpeg 软件解码 fallback，未修改解码链路。
- 新增 6 项选轨测试，覆盖目标命中、明确优先级、普通音质顺序、Dolby Atmos/Dolby Audio 顺序、仅有 Dolby 音轨不回退 192K，以及未知音轨兜底；定向测试 6/6 通过，相关文件定向 Analyze 无问题。
- 全量测试和全量 Analyze 仍受仓库未改动的 Flutter API 兼容错误阻断；本轮未修改版本号、未构建或发布 Release。`git diff --check` 通过，用户未跟踪目录 `tmp/` 保留不变，源码和记录尚未提交。

## 2026-08-30 12:55

- 完成 PiliExo 播放器应用层 Representation fallback：Media3 已启用原生 decoder fallback；仅当 Media3 明确报告视频解码器初始化、解码或容器格式错误时，才把当前 Representation 记录到本次播放 session 的失败列表，并按“同分辨率后续 codec → 更低清晰度”顺序切换。
- fallback 状态不会重复尝试候选，切换时保留当前播放位置、播放意图、倍速和已选音轨；没有加入设备能力预判，不会提前屏蔽 DVH1 等轨道，也不会因未报错黑屏主动降级。所有 Representation 失败后继续沿用原有 Media3 → mpv 兼容播放器回退。
- 新增统一 `PlaybackTelemetry` 数据层，Media3 AnalyticsListener、Format、DecoderCounters 和 mpv/media-kit 实际状态均映射到播放器洞察；详情分类显示当前 Representation、视频/音频 Codec 与 Codec String、Profile/Level、Dolby Vision Profile/Level、HDR、分辨率/帧率、码率、解码器硬软解、采样率、声道、CDN、带宽、缓冲、累计/近期掉帧、状态及 fallback 历史。
- 新增“音频焦点接管”设置，默认开启并即时控制 `AudioSessionHandler`：关闭时忽略 Audio Focus interruption，但耳机/蓝牙音频断开仍自动暂停；Media3 继续关闭自身 Audio Focus 和 becoming-noisy 接管，mpv/media-kit 与 Media3 共用现有 PlPlayerController 音频会话链路。
- 测试覆盖统一洞察数据、Representation 候选不循环、同清晰度优先、位置/播放状态恢复、仅 Dolby 音轨不回退 192K，以及音频焦点设置默认值/持久化。Android `:app:compileDebugKotlin` 和 `HdrMedia3SourceTest` 6 项通过；Telemetry 与自动音轨定向测试 9/9 通过。
- 完整 Flutter 测试仍受仓库既有 Flutter API 兼容错误阻断；本轮未修改版本号、未构建或发布 Release。源码和记录尚未提交，用户未跟踪目录 `tmp/` 保留不变。

## 2026-08-30 13:38

- 发布提交 `840f195bc83b3f45b43d9c494659b134c712b8da` 已推送到 `maxzrb/PiliExo` 的 `main`，创建正式标签 `v26.8.30.3`；应用版本 `26.8.30+3`，Android `versionCode=9`。
- 发布前验证通过：Flutter 3.47.2、全量 Dart 测试 31/31、全量 Analyze 无 error、Android `HdrMedia3SourceTest` 6 项、双 ABI Release 构建、aapt2 包名/版本/ABI/FFmpeg 校验、apksigner V2 正式签名校验和 `git diff --check`。
- GitHub Release：<https://github.com/maxzrb/PiliExo/releases/tag/v26.8.30.3>；ModelScope 双 ABI 资产已同步至 `AerithDream/PiliExo/releases/v26.8.30.3/`，两边的资产长度和哈希均与本地产物一致。
- arm64-v8a：`31,656,238` bytes，SHA-256 `D5E4EA6B6608574BEA728ADA44146D2F5BAC91A7CBAD253CCE0279C32B034CD3`；armeabi-v7a：`31,535,439` bytes，SHA-256 `6A068DD422CDE32B2036F3A3E2CF3EB4F4CC34DEB424183B3B9EB8CFAEE865D2`。
- 本轮未执行真机交互回归；Flutter 3.47.2 本地补丁按流程保留，用户未跟踪目录 `tmp/` 保留不变、未纳入版本控制。
