[修复] 修正普通视频默认画质和移动数据画质选择，严格按照设置列表中的 `8K → HDR Vivid → 杜比视界 → HDR 真彩 …` 顺序降级，不再因画质码数值比较错误默认播放 8K。
[修复] 补齐画质流回填和播放器菜单可用性判断，避免目标画质未直接出现在初始流列表时被错误跳过。
[新增] 增加直播弹幕屏蔽、直播聊天初始位置和消息数量限制等上游功能改进。
[新增] 评论输入支持 LaTeX 符号转换为 Unicode 文本。
[新增] 关机倒计时显示剩余时间，并同步上游相关界面修复。
[同步] 按依赖顺序安全合入 PiliPlus 后续修复、依赖更新和 Cupertino 补丁，保留 PiliExo 的 HDR/Media3、播放器洞察、推荐和更新定制。
[修复] 将 `material_ui` 固定为兼容 PiliPlus 现有底部弹窗和 TabBarView API 的 `1.1.0`，避免 `1.1.1` 移除兼容接口后构建失败。
